sort1 uses: bubble sort

How do you know?: On the sorted list, bubble sort performs very well at Omega(n).

sort2 uses: merge sort

How do you know?: Sort2 performed significantly better on the random list as it can complete the task in O(nlog(n)) while the other 2 programs are slower at O(n2)

sort3 uses: selection sort

How do you know?: On the sorted list, selection sort should be the slowest as it is still operating at O(n2).
